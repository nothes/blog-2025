---
layout: main.liquid
---
    <h2> {{title}} </h2>
    <p>by: {{ author }}, {{ date | date: "%a, %b %d, %Y"}}</p>
<!-- 
    <p>tags: {{tags | join: ", "}}
 -->
    {% for tag in tags %}
    {% if tag != "blog" %}
         <a href="{{siteURL}}/tags/{{ tag }}">{{ tag }}</a>{% if not forloop.last %}, {% endif %}
         {% endif %}
     {% endfor %}
     </p>
  {{ content }}
 
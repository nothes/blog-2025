<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
<link rel="stylesheet" href="/style.css">
<link rel="alternate" type="application/atom+xml" href="{{siteURL}}/feed.xml"/>
</head>
<body>
{% render 'header.liquid' mainURL: siteURL %}

    {{ content }}

{% render 'navigation.liquid' navlinks: navlinks %}
</body>
</html>

